// Data Types: Strings, Numbers, and Booleans.

// Strings are immutable (cannot be changed directly).
let word = 'JavaScript';

// Attempting to modify a string directly (will not work).
// word[0] = 'Y'; // This will not change the string.

// Numbers: Comparing numbers for equality.
let numOne = 3;
let numTwo = 3;
console.log(numOne == numTwo); // Output: true

// Strings: Comparing strings for equality.
let js = 'JavaScript';
let py = 'Python';
console.log(js == py); // Output: false

// Booleans: Comparing boolean values for equality.
let lightOn = true;
let lightOff = false;
console.log(lightOn == lightOff); // Output: false